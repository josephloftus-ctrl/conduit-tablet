# Cloudflared tunnel config TEMPLATE for tablet deployment.
# DO NOT deploy this file directly — it contains PLACEHOLDER values.
# The live config lives at ~/.cloudflared/config.yml on the tablet.
#
# To set up: cloudflared tunnel login && cloudflared tunnel create conduit-tablet
# Then replace TUNNEL_ID_HERE with the actual tunnel ID.

tunnel: TUNNEL_ID_HERE
credentials-file: /data/data/com.termux/files/home/.cloudflared/TUNNEL_ID_HERE.json

ingress:
  - hostname: conduit.josephloftus.com
    service: http://localhost:8080
  - hostname: status.josephloftus.com
    service: http://localhost:8080
  - hostname: steady.josephloftus.com
    service: http://localhost:8090
  - hostname: brief.josephloftus.com
    service: http://localhost:5050
  - hostname: ntfy.josephloftus.com
    service: http://localhost:2586
  - service: http_status:404
